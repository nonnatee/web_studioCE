import { FormController } from "@web/views/form/form_controller";

export class StudioFormController extends FormController {
    // Extends standard controller. Readonly is forced in view props, preventing data modification.
}
